import { Button, Typography } from '@material-ui/core'
import React from 'react'

const Home: React.FC = () => (
	<>
		<Typography variant='h1'> Home Container </Typography>
		<Button color='primary' variant='contained' size='large'>
			Home
		</Button>
	</>
)

export default Home
